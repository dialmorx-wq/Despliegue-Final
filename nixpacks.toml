[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["cd proyecto_springboot/SGA_RPF_COPIA && chmod +x mvnw && ./mvnw -DskipTests package"]

[start]
cmd = "java -Dserver.port=$PORT -jar proyecto_springboot/SGA_RPF_COPIA/target/*.jar"